% Handles key down in edit boxes to make sure the accepted values
% are in a numeric range or a custom restricted input
function [outVal] = checkTextBox(src, evt, cStr, vMin, vMax, preFunc)
    
    if nargin == 5
          
    else
        
    end
end

